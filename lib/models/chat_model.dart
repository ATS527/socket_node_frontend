class ChatModel {
  String name;
  bool isGroup;
  String time;
  String currentMessage;
  int id;

  ChatModel({required this.name,required this.isGroup,required this.currentMessage,required this.id,required this.time});
}